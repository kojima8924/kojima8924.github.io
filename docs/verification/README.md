# 検証記録

このフォルダのログは，実行した時点の成果物に対する記録です．後から変更したHTML・PDFに対する合格判定や，GitHub Pagesへの公開反映を保証するものではありません．新しい検証は実行日・対象・方法と一緒に追記し，古いログを未実行の新結果として扱わないでください．

## 2026-09-25追記：G検定の資格掲載

本人確認済みの取得情報に基づき，Heroの資格欄，詳細経歴，JSON-LDの`hasCredential`へG検定（2026年9月取得）を追加しました．G検定には検証URLを作らず，既存のCredlyリンクはAWS Certified AI Practitionerだけを指す状態を維持しています．後続で主担当が両PDFも再生成し，採用向け2ページ・完全版7ページを確認しました．

- `python scripts/check_portfolio.py`はエラー0・警告0（ID20件・画像33枚・外部リンク18件）でした．
- `python -m unittest discover -s tests -v`は41件成功，`python scripts/import_figures.py --check`は更新要0件・欠損0件でした．
- JSON-LDをPython標準ライブラリで解析し，`G検定`，`dateCreated: 2026-09`，URLなしの組合せを確認しました．
- PlaywrightでPC 1440×1000とスマホ相当390×844のライト／ダークを撮影し，Heroと詳細経歴の資格欄を目視しました．4条件とも指定テーマが実際に適用され，文字の切れ・重なりはありません．ページ全体，Hero資格欄，詳細資格欄の`scrollWidth`と`clientWidth`は各条件で一致し，横はみ出しもありませんでした．
- 確認画像はGit管理対象外の`tmp/gkentei-20260925/`に，`pc|mobile` × `light|dark` × `hero|career-credentials`の8枚を保存しました．外部URLを変更していないため，外部到達検査は再実行していません．
- 主担当も上記8画像を確認し，さらにPDFを90dpiで全9ページ描画して資格の欠落・文字の重なり・画像の切れがないことを確認しました．既存のカードの複数ページへの継続は維持しています．PDF本文抽出でも両版にG検定が含まれます．作成段階ではcommit・pushせず，同日の本人の追加依頼により公開リポジトリへのpush対象としました．

## 2026-09-20の構成見直し：今回の実行結果

対象はローカル作業ツリーの改修版です．この記録時点ではcommit・push・公開反映は行っていません．以下は今回の操作・検査の記録であり，下記の旧ログ3ファイルを今回の結果で上書きしてはいません．

### 画面・操作の確認

CUA経由のChromiumブラウザーで，PC 1440×1000とスマホ相当390×844のライト／ダークを撮影し，目視で確認しました．画面幅の変更による検証であり，Android実機・エミュレータの検証ではありません．

| 対象 | 実際に確認した内容 |
|---|---|
| セクション構成 | 研究 → 受託 → 自力CG → AI活用作品 → 詳細経歴 → 補足作品 → 連絡先の順に表示 |
| CG3作品 | 画像付きで常時表示，3画像の表示比率を統一．研究・AI作品の補足説明は折りたたみ |
| 画面幅 | 320／360／390／768／1024／1440px × ライト／ダークの12条件で，`document.documentElement.scrollWidth == document.documentElement.clientWidth`．スクロールバー分を除く本文領域で横はみ出しなし |
| 図の拡大 | スマホ相当画面で初期幅900px → ＋操作で1350px → 全体表示で370pxを測定．横スクロール可能 |
| 図の閉じる操作 | Escで閉じ，背景の`inert`が解除され，起点へフォーカスが戻ることを確認 |
| モバイルメニュー | 開くアニメーションの途中で項目を選択しても，閉じて該当箇所へ移動する修正を実操作で確認 |
| 動画モーダル | 表示と閉じる操作，起点へのフォーカス復帰を確認．844×390の横向き相当でも閉じるボタンまで画面内に収まる |

動画モーダル内のニコニコ動画埋め込みフレームは，今回のブラウザーでは黒画面になり，動画の実再生は確認できていません．元動画ページを開くリンクは維持しています．モーダルの正常表示を，動画配信側を含む再生成功とは扱いません．

画面証拠はGit管理対象外の`tmp/improve-20260920/`に保存しました．

- `pc-light.png`／`pc-dark.png`／`mobile-light.png`／`mobile-dark.png`
- `pc-light-cg.png`／`pc-dark-ai.png`／`mobile-light-cg.png`
- `mobile-diagram-zoom.png`／`mobile-landscape-video.png`

### 自動検査・OGP

| 検査 | 今回の結果 |
|---|---|
| `check_portfolio.py`内部検査 | エラー0・警告0．ID20件・画像32枚・外部リンク18件 |
| `check_portfolio.py --external` | 最終実行はエラー0・警告0，外部18リンクすべて到達 |
| `import_figures.py --check` | 更新要0件・欠損0件．旧`clinic-results.svg`と参照元PDFも保持して照合 |
| `python -m unittest discover -s tests -v` | 41件成功．PDF内の生成サーバーURIを公開URLへ変換し，外部URL・`mailto:`・内部移動を維持する後処理も検証 |
| OGP | 1200×630pxで再生成し，研究／受託／自力CG／AI活用の内容と外観を目視確認 |

外部リンク検査の最初の実行では，sandboxのネットワーク制約により18件の警告が出ました．ネットワーク制約外で再実行し，上表の全18件到達を確認しました．最初の失敗をリンク切れと決めつけたり，検査せず成功扱いにしたりはしていません．

今回版へのaxe-coreとW3C Nuの再実行は行っていません．下記の旧版のviolations 0件などを，今回版の結果として流用しません．

### PDFの最終受入状況

最終再生成後の実測値は，採用向け2ページ・332,469 bytes（約325 KiB），完全版7ページ・1,056,171 bytes（約1.01 MiB）です．全ページをPopplerで画像化して確認しました．本文・図のクリップ，見出しだけの改ページ，画像から孤立したキャプションはなく，最終ページも連絡先だけの空白ページにならないよう調整しました．完全版でページをまたぐTriviumの追加画像は，キャプションに作品名を明記して文脈を維持しています．確認画像は`tmp/improve-20260920/full-accepted-*.png`と`summary-accepted-*.png`です．

研究・CGの自力実装，受託・代表OSSのAIへの実装委任，条件付きの指標が，両PDFに残っていることを確認しました．リンク注釈を検査し，生成サーバーの`127.0.0.1`は両PDFとも0件，シャドウマッピングの画像リンクは`https://kojima8924.github.io/media/shadowmap.jpg`になっています．Webで重複する経歴末尾の総括文は印刷のみ省略し，冒頭・作品ごとの担当説明は保持しています．

## 2026-09-20追記：研究比較図の切り出し範囲

正式版コミット`836012a`の後，`research-recon.webp`を元の高解像度ポスターから再切り出ししました．上の「接触点」注記・矢印と，右の導電率カラーバー（0・50%・100%）まで含め，1112×584pxに変更しています．画像内容の描き直しや生成AIによる補完はしていません．ブラウザーに旧画像が残らないよう画像URLに版識別子を付けました．元画像と切り出し手順は`scripts/crop_research_recon.py`に記録しています．

完全版PDFも再生成し，7ページ・1,053,574 bytesです．対象の1ページ目とブラウザーの拡大表示を目視確認し，2〜7ページは前版の検証画像と画素単位で同一でした．採用向けPDFにはこの比較画像を掲載していないため，変更していません．内部検査・図版同期・41件のテストも正常です．

## 2026-09-20追記：Triviumの共有画像を選定

共有された21枚と既存素材を比較し，LINEプロフィール・操作メニュー（65939）とWeb四択問題（65936）を原寸・無加工で追加しました．ヒント（65941）と教材提案（65944）は，同じ画面の既存高解像度版を維持しています．常時表示はLINEの2枚，折りたたみ内はWebダッシュボード（デモデータ明示）・四択問題・教材提案の3枚です．横長枠で小さくなっていた補足画像を縦長枠へ変更し，狭い画面では折り返します．

極端に低解像度の長尺画像，初期化直後の画面，未整形の小数（65947・65949），ヒント要求と応答の不一致（65925）などは採用していません．これらは共有画像の観察結果であり，現行サービスで再現確認・修正したことを意味しません．共有原本と旧素材は削除せず，`.codex-remote-attachments/`をGit対象外にしました．

- PC 1440×1000・スマホ相当390×844のライト／ダークを目視確認しました．プロフィール画像の拡大・閉じる操作，補足ギャラリーの展開も確認しています．
- 320／390／768／1440pxの両テーマで，展開状態のページ全体・キャプションの横はみ出しなし，掲載5画像の読込成功を確認しました．Android実機・エミュレータでの追加検証は行っていません．
- 内部検査はエラー0・警告0（画像33枚），回帰テスト41件成功，図版同期は更新要0・欠損0でした．外部URLは変更しておらず，今回の外部到達検査は再実行していません．
- 完全版PDFを再生成し，7ページ・1,097,335 bytesでした．全7ページをPopplerで描画して確認し，3ページ目の主画像2枚，4ページ目の補足3枚とキャプションに切れ・重なりはありません．生成サーバーを指すリンクは0件です．採用向けPDFは変更箇所を掲載しないため更新不要，OGPも変更不要と確認しました．
- 確認画像・画面幅検査結果はGit対象外の`tmp/trivium-20260920/`に保存しました．この作業ではpush・公開は行っていません．

## 2026-09-20追記：受託案件の改善手法を明記

図版の根拠資料`facts.md`の受託節と公表済みCVを照合し，2つの指標の近くにTop-K 4→10，同一FAQ・切替条件維持，確認項目13/26→21/26，公式サイトへの追加検索3/7問→0/7問を記載しました．応答時間短縮との因果は「要因と考えられます」と推定として示し，各1回の限定比較，別途のナレッジ修正との区別を保っています．新しい実測や顧客データの取得は行っていません．

PC 1440×1000・スマホ相当390×844の両テーマで，手法・各指標の説明・測定条件を目視確認しました．320pxでも両テーマのページ全体に横はみ出しはありません．帯図は従来どおり図の内部で横スクロールできます．内部検査・図版同期・回帰テスト41件も正常です．外部URL・OGP・帯図本体は変更不要と確認し，外部到達検査は再実行していません．

採用向けPDFは追記直後に3ページとなったため，クリニックのタイトルと状態ラベルを横並びにし，数値カードの余白を詰めて2ページへ戻しました．本文削除や本文フォントの縮小はしていません．最終版は採用向け2ページ・339,989 bytes，完全版7ページ・1,100,556 bytesです．全9ページをPopplerで描画して目視確認し，対象ページの切れ・重なりや，他ページへの悪影響はありません．確認画像は`tmp/clinic-method-20260920/`の`*-accepted-*.png`とブラウザー画像に保存しています．push・公開は行っていません．

## 自動検査の再実行

リポジトリルートから実行します．

```powershell
python scripts/check_portfolio.py
python scripts/check_portfolio.py --external
python -m unittest discover -s tests -v
python scripts/import_figures.py --check
```

`import_figures.py --check`はPyMuPDFで参照元PDFからSVGをメモリ内に生成し，改行を正規化した既存SVGと内容を比較します．参照元PDFまたは出力SVGの欠損，生成内容との差異があれば終了コード1を返します．mtimeは判定に使いません．

通常変換でも対象PDFを事前にすべて検証し，欠損があれば何も変換しません．SVGは出力先と同じディレクトリの一時ファイルへ書き終えてから置換し，書き込み失敗時は既存SVGを保持します．

## 2026-09-19の保存済み結果（旧構成）

以下は外部レビューを受けた改修コミット時点の結果です．`check_portfolio.log`，`w3c-nu.json`，`axe-core.json`はこの時点の証拠として保持しています．2026-09-20以降の構成変更に対して同じ検査を実行済みとは意味しません．

| ファイル | 内容 | 当時の結果 |
|---|---|---|
| `check_portfolio.log` | `python scripts/check_portfolio.py --external`の出力 | エラー0・警告0（画像43枚・外部リンク18件全到達） |
| `w3c-nu.json` | W3C Nu HTML validator（https://validator.w3.org/nu/?out=json）の生JSON | error 0件・warning 0件（infoのみ17件） |
| `axe-core.json` | axe-core 4.10.2（dark／light両テーマ・details全展開．初回描画の`color_scheme`でテーマを確定） | violations 0件（両テーマ） |

当時は幅360／390／768／1280pxで`scrollWidth <= innerWidth`を確認しました．帯図の横スクロールは`.fig-band`内に収めていました．

当時のPDF紙面受入検査は，画像とキャプションの同居・孤立見出しなし・最終ページ空白50%未満を，ページ画像で目視しました．

- 採用向け2ページ: 帯図と受託案件の構成図を掲載し，重複する実機画面と段落を省略．当時の受入検査は合格．
- 完全版9ページ: 帯図と実機画面を掲載し，受託案件を見出しごと新ページへ配置．LINE実機フロー2枚は高さ110mm．当時は空枠断片・孤立見出し・孤立キャプションがなく，画像の実効解像度はすべて100dpi以上．帯図内の小さなロゴは再圧縮せず保持．

2026-09-18の帯図追加時には，darkのつもりの検査が実際にはlightで動いていました（`data-bs-theme`を後から変更していたため）．2026-09-19に`color_scheme`で初回描画からdarkにして再検査し，帯図キャプションのコントラスト不足（白地に`#b9c7d8`，1.71）を発見して修正しました．テーマ確認では，指定値だけでなく実際の表示・適用状態も確認してください．
